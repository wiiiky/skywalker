# socks5代理的配置

socks5:
  bindAddr: 127.0.0.1
  bindPort: 8080
  autoStart: true
  clientAgent: socks
  clientConfig:
    version: 5		#如果不指定则同时支持socks4和socks5
    username: ""
    password: ""	#socks5代理支持用户认证，只支持用户名/密码方式
  serverAgent: direct
