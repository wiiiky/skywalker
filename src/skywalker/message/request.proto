syntax = "proto2";
package message;

enum RequestType {
    STATUS = 1;   /* 当前状态 */
    START = 2;    /* 启动服务 */
    STOP = 3;     /* 停止服务 */
    RESTART = 4;  /* 重启服务 */
    INFO = 5;     /* 服务详细信息 */
}

message CommonRequest {
    repeated string name = 1;
}

message Request {
    required int32 version = 1;
    required RequestType type = 2;
    optional CommonRequest common = 3;
}
